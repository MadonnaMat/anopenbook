= form_for @edit do |f|
  - if @edit.errors.any?
    #error_explanation
      %h2= "#{pluralize(@edit.errors.count, "error")} prohibited this edit from being saved:"
      %ul
        - @edit.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
